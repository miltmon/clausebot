export default function ComplianceFooter() {
  return (
    <footer className="px-4 py-6 text-center text-xs text-muted-foreground">
      ClauseBot supports compliance decisions; it does not replace the engineer of record.
    </footer>
  );
}